spring:
  application:
    #注意：name 避免使用下划线：_
    name: com-live-test-MicroService-SpringCloudNetflix-eureka-discovery-provider
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}